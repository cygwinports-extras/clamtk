--- origsrc/clamtk-4.02/lib/Update.pm	2008-10-26 07:01:14.000000000 -0500
+++ src/clamtk-4.02/lib/Update.pm	2008-11-04 17:07:06.397494300 -0600
@@ -49,7 +49,7 @@
 
     my $sig_box = Gtk2::CheckButton->new();
     $sig_box->set_active(FALSE);
-    if ( $> != 0 ) {
+    if ( $> != 0  || $^O == 'cygwin' ) {
         $sig_box->set_sensitive(FALSE);
         $tt->set_tip( $sig_box, gettext('You must be root to enable this.') );
     }
@@ -62,7 +62,7 @@
         $user_can = 1;
     }
 
-    if ( $> == 0 || $user_can ) {
+    if ( $> == 0 || $^O == 'cygwin' || $user_can ) {
         push @{ $update_list->{data} },
             [
             gettext("Signature updates"),
