--- origsrc/clamtk-4.28/lib/Update.pm	2010-08-15 08:04:29.000000000 -0500
+++ src/clamtk-4.28/lib/Update.pm	2010-08-22 21:57:43.643838700 -0500
@@ -63,7 +63,7 @@ sub update_dialog {
 
     my $sig_box = Gtk2::CheckButton->new();
     $sig_box->set_active(FALSE);
-    if ( $> != 0 ) {
+    if ( $> != 0 && $^O != 'cygwin' ) {
         $sig_box->set_sensitive(FALSE);
         $tt->set_tip( $sig_box, gettext('You must be root to enable this.') );
     }
@@ -79,7 +79,7 @@ sub update_dialog {
 
     # Add signature updates as an option IF
     # the user is root, or has opted to do it manually
-    if ( $> == 0 || $user_can ) {
+    if ( $> == 0 || $^O == 'cygwin' || $user_can ) {
         if ( $paths->{freshclam} ) {
             push @{ $update_list->{data} },
                 [
